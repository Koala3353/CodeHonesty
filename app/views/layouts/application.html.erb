<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hackatime - Code Time Tracker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Hackatime">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="WakaTime-compatible coding time tracker for Hack Club">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <nav class="bg-white shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to "⏱️ Hackatime", root_path, class: "text-xl font-bold text-indigo-600" %>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <%= link_to "Leaderboards", leaderboards_path, class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <%= link_to "About", about_path, class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
            </div>
          </div>
          <div class="flex items-center">
            <% if logged_in? %>
              <span class="text-gray-700 mr-4">👋 <%= current_user.display_username %></span>
              <%= link_to "Settings", settings_users_path, class: "text-gray-500 hover:text-gray-700 mr-4" %>
              <% if current_user.admin? %>
                <%= link_to "Admin", admin_root_path, class: "text-red-500 hover:text-red-700 mr-4" %>
              <% end %>
              <%= button_to "Logout", destroy_session_path, method: :delete, class: "bg-gray-200 hover:bg-gray-300 text-gray-700 px-4 py-2 rounded-md text-sm font-medium" %>
            <% else %>
              <%= link_to "Login", new_session_path, class: "bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-md text-sm font-medium" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
      <% if flash[:notice] %>
        <div class="mb-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= flash[:notice] %></span>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="mb-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= flash[:alert] %></span>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="bg-white border-t mt-auto">
      <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between text-gray-500 text-sm">
          <div>
            Built with ❤️ for <a href="https://hackclub.com/" class="text-indigo-600 hover:underline">Hack Club</a>
          </div>
          <div class="space-x-4">
            <%= link_to "Privacy", privacy_path, class: "hover:text-gray-700" %>
            <%= link_to "Terms", terms_path, class: "hover:text-gray-700" %>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
