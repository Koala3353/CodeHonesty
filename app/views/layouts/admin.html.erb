<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin - Hackatime" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 min-h-screen">
    <nav class="bg-gray-800">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to "⚙️ Hackatime Admin", admin_root_path, class: "text-xl font-bold text-white" %>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <%= link_to "Dashboard", admin_root_path, class: "text-gray-300 hover:text-white inline-flex items-center px-1 pt-1 text-sm font-medium" %>
              <%= link_to "Users", admin_users_path, class: "text-gray-300 hover:text-white inline-flex items-center px-1 pt-1 text-sm font-medium" %>
              <%= link_to "GoodJob", good_job_path, class: "text-gray-300 hover:text-white inline-flex items-center px-1 pt-1 text-sm font-medium" %>
            </div>
          </div>
          <div class="flex items-center">
            <span class="text-gray-300 mr-4"><%= current_user.display_username %></span>
            <%= link_to "← Back to App", root_path, class: "text-gray-300 hover:text-white" %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
      <% if flash[:notice] %>
        <div class="mb-4 bg-green-600 text-white px-4 py-3 rounded" role="alert">
          <span class="block sm:inline"><%= flash[:notice] %></span>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="mb-4 bg-red-600 text-white px-4 py-3 rounded" role="alert">
          <span class="block sm:inline"><%= flash[:alert] %></span>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>